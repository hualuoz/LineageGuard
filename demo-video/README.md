# LineageGuard demo video

This directory contains the reproducible Remotion source for the 60-second LineageGuard product demo.

```bash
npm install
npm run lint
npm run render
```

The rendered H.264 video is written to `out/lineageguard-demo.mp4`. Rendered files are intentionally ignored by Git.
